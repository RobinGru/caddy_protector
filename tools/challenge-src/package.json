{
  "name": "caddy-protector-challenge",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/challenge.js --bundle --format=iife --global-name=CaddyProtector --platform=browser --target=es2020 --outfile=dist/challenge.bundle.js",
    "test": "node test-vector.js"
  },
  "dependencies": {
    "blake3-bao": "^1.3.4"
  },
  "devDependencies": {
    "esbuild": "^0.25.4"
  }
}
